# Example only. Run headless-virtual-display-setup for target-specific values.
capture = kwin
encoder = vaapi
output_name = CONFIGURED_CONNECTOR
global_prep_cmd = [{"do":"CONFIGURED_PREFIX/bin/virtual-display sunshine-up","undo":""}]
