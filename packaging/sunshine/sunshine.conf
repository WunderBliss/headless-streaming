# Merge these keys into ~/.config/sunshine/sunshine.conf after backing it up.
capture = kwin
encoder = vaapi
output_name = DP-1
global_prep_cmd = [{"do":"/usr/local/bin/virtual-display sunshine-up","undo":""}]
